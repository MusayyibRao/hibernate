spring:
  datasource:
    url: jdbc:mysql://localhost/userpost?createDatabaseIfNotExist=true
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update     
   